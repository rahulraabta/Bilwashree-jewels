[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Redirect all routes to index.html for SPA-style navigation
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
